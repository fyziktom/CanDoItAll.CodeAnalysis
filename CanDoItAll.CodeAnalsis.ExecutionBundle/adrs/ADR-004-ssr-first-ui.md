# ADR-004 — SSR-first UI

## Status
Accepted

## Decision

The first UI is a Blazor Web App with SSR as the default mode.
No heavy client-side graph framework is required for the first wave.

## Rationale

The goal is inspection and verification, not visual flourish.
SSR keeps the first UI easier to maintain and later transplant.
