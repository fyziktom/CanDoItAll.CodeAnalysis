# Implementation prompt

Execute the comparison study exactly as the bundle describes. Freeze the scenarios first, gather focused-context evidence and SharpTools evidence separately, normalize the metrics, and record the findings in the bundle instead of leaving them in chat.
