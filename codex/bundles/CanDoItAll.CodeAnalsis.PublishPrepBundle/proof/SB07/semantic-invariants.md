# SB07 Semantic Invariants

- No external publish was performed.
- The package set contains eight reusable `CanDoItAll.CodeAnalytics.*` library packages and excludes Web, tools, tests, fixtures, Codex proof, and local outputs.
- The Web project remains a desktop-large sandbox app, not a reusable package.
- The future `CanDoItAll.Mcp.CodeAnalytics` driver remains reference-only and was not implemented.
- Package descriptions match proven behavior: static EF Core persistence metadata analysis is documented, but runtime EF query optimization is not claimed.
- MIT license metadata is consistent across package metadata and repository `LICENSE`.
