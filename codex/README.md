# Codex Notes

The active publishing-preparation bundle lives under `codex/bundles/CanDoItAll.CodeAnalsis.PublishPrepBundle`.

Repo-local Codex assets in this folder should stay lightweight and portable so the future `CanDoItAll.Mcp.CodeAnalytics` host integration only needs thin glue.

Use `validation-matrix.md` for the release guardrail command matrix, expected slow-test behavior, and package command.
