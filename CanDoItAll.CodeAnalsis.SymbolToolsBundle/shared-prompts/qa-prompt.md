# QA prompt

Validate the symbol-tool parity work by checking:

- exact symbol search works for both types and members
- definition rendering includes correct path and line metadata
- members, implementations, and references are deterministic and bounded
- the symbol explorer page exposes the new capabilities clearly
- build, tests, Playwright proof, and rerun comparison evidence are all recorded
